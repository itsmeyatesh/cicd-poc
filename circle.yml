run: 
  - mvn clean install deploy

store_artifacts:
       path: target/GreetingService-0.0.1-SNAPSHOT.jar
       
deployment:
  docker_hub:
    branch: master
    commands: 
      - $DOCKER_TRIGGER